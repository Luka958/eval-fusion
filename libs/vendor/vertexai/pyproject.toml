[project]
name = "eval-fusion-vertexai"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.9,<3.13"
authors = [
  { name = "Luka958", email = "lp53523@fer.hr" },
]

dependencies = [
  "eval-fusion-core",
  "eval-fusion-openai",
  "python-decouple>=3.8,<4.0.0",
  "vertexai>=1.71.1,<2.0.0",
  "google-auth>=2.38.0,<3.0.0",
]

[tool.uv.sources]
eval-fusion-core = { path = "../../core", editable = true }
eval-fusion-openai = { path = "../openai", editable = true }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
